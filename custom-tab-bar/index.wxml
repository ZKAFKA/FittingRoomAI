<view class="tab-bar">
  <view class="tab-item left" bindtap="switchTab" data-index="0">
    <image class="tab-icon" src="{{active === 0 ? '/assets/icons/tryon-active.png' : '/assets/icons/tryon.png'}}"></image>
    <text class="tab-text {{active === 0 ? 'active' : ''}}">{{langData.navigation.tryon || '试衣'}}</text>
  </view>
  
  <view class="tab-divider"></view>
  
  <view class="tab-item right" bindtap="switchTab" data-index="1">
    <image class="tab-icon" src="{{active === 1 ? '/assets/icons/profile-active.png' : '/assets/icons/profile.png'}}"></image>
    <text class="tab-text {{active === 1 ? 'active' : ''}}">{{langData.navigation.profile || '我的'}}</text>
  </view>
</view>